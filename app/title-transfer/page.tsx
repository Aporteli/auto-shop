import TitleTransferPage from '@/components/TitleTransferPage';

export const metadata = {
  title: 'Title transfer | AutoShop',
  description: 'Reserve an instant vehicle ownership transfer. Pay only the reservation fee now.',
};

export default function TitleTransferRoutePage() {
  return <TitleTransferPage />;
}
